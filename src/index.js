import React from 'react';
import ReactDOM from 'react-dom';

import Selector from './D3Selector';

ReactDOM.render(
  <React.StrictMode>
    <Selector />
  </React.StrictMode>,
  document.getElementById('ReactRoot')
);
